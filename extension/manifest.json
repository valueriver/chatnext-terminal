{
  "manifest_version": 3,
  "name": "browser-use",
  "version": "0.1.0",
  "description": "Drive Chrome via the DevTools Protocol over one WebSocket. Point it at any hub URL; the controller speaks raw CDP.",
  "permissions": ["debugger", "tabs", "storage", "alarms"],
  "host_permissions": ["<all_urls>"],
  "background": { "service_worker": "background.js" },
  "action": { "default_popup": "popup.html", "default_title": "browser-use" }
}
