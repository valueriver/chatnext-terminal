{
  "name": "one-worker",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:ui": "npm --prefix ../ui run build",
    "dev": "wrangler dev --port 9506",
    "predeploy": "npm run build:ui",
    "deploy": "wrangler deploy",
    "start": "wrangler dev --port 9506"
  },
  "devDependencies": {
    "wrangler": "^4.35.0"
  }
}
